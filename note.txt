<h1 className='font-medium'>VRef — JavaScript library for reactive mutation and change tracking.</h1>
// Read MDX file on server
   // Bundle MDX (GFM tables enabled)
  // const { code } = await bundleMDX({
  //   source,
  //   mdxOptions(options) {
  //     options.remarkPlugins = [...(options.remarkPlugins ?? []), remarkGfm];
  //     return options;
  //   },
  // });

  // Render as a React component
  // const MdxComponent = getMDXComponent(code);